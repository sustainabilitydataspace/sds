"""SDS semantic-atomization subsystem.

VARCH work package for deterministic semantic-atomization.
VARCH-0 lands the deterministic foundation under ``profiles`` only; DB schema,
resolver, and public APIs arrive in later sub-slices.
"""
