# E11 Official URL

Official project communication website:

- `https://sustainabilitydataspace.com/`

Evidence status: user-provided canonical URL.

Register status: `official-url-recorded`.

Availability observations:

- On 2026-05-30, a manual workspace check returned HTTP 404 for this URL.
- On 2026-05-31, a repeat manual workspace check returned HTTP 404 for this URL.
- On 2026-06-01, a repeat manual workspace check returned HTTP 404 for this URL.
- On 2026-06-08, HEAD and GET workspace checks both returned HTTP 404 for this
  URL.
- On 2026-06-23, HEAD and GET workspace checks both returned HTTP 200 for this
  URL.

These notes record dated availability observations only. The 2026-06-23 HTTP
200 observation proves reachability at that date; it does not by itself close
content acceptance, analytics, E12 impact evidence, or full subsidy closure.

Maintenance observations:

- On 2026-07-22, the public legal pages were corrected through authenticated
  WordPress/Elementor administration: `aviso-legal`, `politica-de-cookies`, and
  `politica-de-seguridad` were aligned to the Elementor header/footer template,
  and scoped Customizer CSS restored legal body text to normal weight on those
  pages plus `politica-de-privacidad`.
- Browser QA on desktop and mobile confirmed one visible `h1` per legal page,
  no native `.page-header` wrapper, no computed heading underline/bottom border,
  and legal body text at `font-weight: 400`.

This file records the official publication target. It does not claim that the
local `web/sds_website/` test artifact is official evidence.
